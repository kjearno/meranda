import { reducer } from "./reducer";
import * as operations from "./operations";
import * as selectors from "./selectors";

export {
  reducer as profileReducer,
  operations as profileOperations,
  selectors as profileSelectors
};
